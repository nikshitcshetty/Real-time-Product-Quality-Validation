follow steps to run the project


1.Install Required Dependencies

2.Make sure you have Node.js installed. If not, download and install it from nodejs.org.

3.Then, open a terminal and install Express.js:
 Set Up the Backend Server (Node.js)

4.Create a new folder for your project.
   type the  command in command prompt/power shell
   npm install express cors

5.Inside the folder, create a file named server.js and paste the following code:
 Start the backend server by running:
 Set Up the Frontend Dashboard

6.In the same project folder, create three files:

index.html

styles.css

script.js

 Run the Project:

*Start the backend:
node server.js
* Open index.html in a web browser.
* The dashboard should now display real-time product validation data.

7.deploy the project in IBM /any other cloud platform....
